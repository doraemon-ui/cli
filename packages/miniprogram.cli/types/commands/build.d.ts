import { Options } from '../options';
import { PluginAPI } from '../PluginAPI';
export default function build(api: PluginAPI, options: Options): void;
export declare const defaultModes: {
    build: string;
};
