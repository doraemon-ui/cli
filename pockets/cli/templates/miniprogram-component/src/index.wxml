<view
  data-dora-theme="{{ darkmode }}"
  class="dora-class {{ classes.wrap }}"
  hover-class="dora-hover-class {{ !disabled ? classes.hover : 'none' }}"
  bindtap="onClick"
>
  <slot></slot>
</view>